# Build output and derived state.
node_modules/
dist/
_site/
data/
*.tsbuildinfo
*.tgz
packages/cms/admin/static/editor.js
# esbuild's other output, committed rather than gitignored so a site gets a
# highlighter without a build step. Written by `pnpm --filter @geekity/cms
# build:highlight`.
packages/cms/themes/default/static/highlight.js

# Written by tools, not by hand.
pnpm-lock.yaml
CHANGELOG.md

# Backlog.md owns these files; the CLI writes them and prettier would fight it.
backlog/

# Content is written by the CMS's own document writer, in a fixed key order and
# a fixed shape. Reformatting it would make every fixture diff against the
# bytes the writer produces, which is exactly what the round-trip tests assert.
packages/cms/test/fixtures/
apps/demo/content/
packages/cms/templates/site/content/

# Claude Code writes this on the machine it runs on; it is not in the repo.
.claude/settings.local.json
