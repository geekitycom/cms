{# The plain text body of the confirmation. It carries no link and no token:
   it is a notice, and a notice that could let somebody in would be a second
   reset link nobody asked for. #}
Hello {{ username }},

The password for {{ username }} on {{ site.title }} has just been set through a
reset link.{% if signedOut %} Every browser that was signed in as {{ username }} was signed out.{% endif %}

If that was you, there is nothing to do.

If it was not, somebody has your email and your account. Sign in at
{{ baseUrl }}/admin/login with the new password if you have it, and ask whoever
runs this site to look at the account if you do not.

--
{{ site.title }}
{{ baseUrl }}
