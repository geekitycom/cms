{# The subject of the digest a batched user gets. One line, and it says how
   many, because the count is the thing that decides whether to open it now. #}
{{ total }} comment{% if total != 1 %}s{% endif %} waiting on {{ site.title }}
