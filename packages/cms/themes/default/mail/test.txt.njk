{# The plain text body of the test message. Every message has one of these. #}
This is a test message from {{ site.title }}.

If you are reading it, {{ baseUrl }} can send email, and the password resets,
moderation notices and contact messages that go through the same service will
arrive as well.

Nothing was sent to anybody else, and nothing about this message was stored.

--
{{ site.title }}
{{ baseUrl }}
